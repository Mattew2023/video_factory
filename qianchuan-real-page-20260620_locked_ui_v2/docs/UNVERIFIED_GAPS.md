# Unverified Gaps

以下内容缺少足够截图、DOM 或真实数据逻辑依据。第三阶段不得凭空补全，只能在取得新参考后实现。

## 图表与数据逻辑

- 趋势图真实算法：缺少净成交金额、消耗、ROI、综合成本等指标的真实生成与聚合规则。
- tooltip 规则：缺少 hover 命中范围、时间区间、指标顺序、事件明细、空值显示、定位和避让规则。
- 粒度切换规则：每分钟粒度、5分钟粒度、30分钟粒度的采样方式与时间边界未确认。
- 漏斗比例计算：右侧漏斗条宽度、画布连接形态和极端值显示规则未确认。
- 成交渠道切换：观看次数与成交金额模式的数据口径、排序规则和百分比计算未确认。

## 样式与交互

- 表格样式依据不足：目前没有足够依据复刻表格表头、行高、选中态、分页、空态和滚动区域。
- 红字提示触发条件：只看到趋势 tooltip 中存在“存在2次素材调整”类型文本，完整红字文案、数量来源和触发条件未确认。
- 评论列表有评论状态样式：评论版截图可作状态补充，但评论头像、昵称、时间、内容折行、滚动和高亮规则仍未确认。
- 弹窗与筛选器展开态：数据口径说明、指标配置、筛选器、明细弹窗、调控弹窗均缺少展开截图。
- 右上角状态区字段边界：直播状态枚举、时长过长、开播时间换行/截断、刷新反馈和全屏切换显示规则未确认。
- 响应式规则：宽屏、窄屏、浏览器缩放、全屏状态下的模块压缩和溢出规则未确认。

## 内容边界

- 主播信息异常态：主播名过长、无头像、头像加载失败、非结束状态展示规则未确认。
- 公告多条状态：公告轮播数量、关闭后是否消失、按钮点击后的跳转或弹层未确认。
- 右侧卡片分页：成交渠道图例超过当前显示数量时的翻页、滚动或截断规则未确认。
- 旧罗盘风格边界：`reference-luopan-dashboard-v2.png.png` 只作为 P2 旧风格参考，不能用于补全千川首版视觉。
